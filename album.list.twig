<!DOCTYPE html>
<html lang="{{ settings.language }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>{{ title }} - {{ site_title }}</title>
        <link rel="stylesheet" href="{{themepath}}/css/normalize.css">
        <link rel="stylesheet" href="{{themepath}}/css/style.css">
    </head>
    <body>
        <header>
            <h1>{{ site_title }}</h1>
            {% include "components/nav.twig" %}
        </header>
        <main>
            <h2>Gallery List</h2>
            <ul class="album-list">
                {% for album in albums %}
                    <li class="album-entry">
                        <a href="/gallery/{{ album.slug }}">
                            {% if album.cover %}
                                <img src="{{ album.cover }}" alt="{{ album.title }}" />
                            {% endif %}
                            <h2>{{ album.title }}</h2>
                        </a>
                        <p>{{ (album.description|slice(0, 120) ~ '...')|raw  }}</p>                        
                    </li>
                {% endfor %}
            </ul>
        </main>
    </body>
</html>